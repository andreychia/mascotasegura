DATABASE_URL=postgresql://mascotasegura:CHANGE_ME@127.0.0.1:55432/mascotasegura
APP_URL=http://localhost:3100
# Only set to true behind HTTPS. Set true for any hosted environment.
COOKIE_SECURE=false
# The only account allowed to open the administration panel.
SUPER_ADMIN_EMAIL=admin@example.com
# Transactional email for one-time password recovery links.
RESEND_API_KEY=re_CHANGE_ME
RESET_EMAIL_FROM=MascotaSegura <cuentas@correo.tudominio.com>
# Alternative when there is no custom domain: Gmail plus a 16-character App Password.
SMTP_USER=tu-cuenta@gmail.com
SMTP_APP_PASSWORD=CHANGE_ME
# Stripe test keys for local/dev; use live keys only in production.
STRIPE_SECRET_KEY=sk_test_CHANGE_ME
STRIPE_WEBHOOK_SECRET=whsec_CHANGE_ME
